{
    "buildCommand": "flutter build web --wasm",
    "outputDirectory": "build/web",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}